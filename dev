#!/usr/bin/env python3
"""Agent entrypoint; discovery requires only the Python standard library."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "scripts" / "dev"))
from agent_dev import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
