apiVersion: v1
kind: Service
metadata:
  name: deneb
  labels:
    app: deneb
spec:
  type: ClusterIP
  selector:
    app: deneb
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
