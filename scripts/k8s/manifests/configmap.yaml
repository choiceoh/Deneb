apiVersion: v1
kind: ConfigMap
metadata:
  name: deneb-config
  labels:
    app: deneb
data:
  deneb.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.deneb/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Deneb Assistant",
            "workspace": "~/.deneb/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Deneb Assistant

    You are a helpful AI assistant running in Kubernetes.
