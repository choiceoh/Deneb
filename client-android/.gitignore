# Build outputs and caches for the vendored Android client (client-android/app).
# The vendored Kai source is reproducible via vendor-kai.sh at the commit pinned
# in VENDOR.txt; build artifacts and machine-specific config are not committed.
/app/.gradle/
/app/.kotlin/
/app/build/
/app/*/build/
/app/local.properties
/app/captures/
/.vendor.*
*.apk
*.aab
