<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

   <uses-permission android:name="android.permission.INTERNET"/>
   <!-- ConnectivityManager.registerDefaultNetworkCallback (BackgroundConnectionPolicy, M2). -->
   <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
   <uses-permission android:name="android.permission.READ_CALENDAR"/>
   <uses-permission android:name="android.permission.WRITE_CALENDAR"/>
   <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
   <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
   <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
   <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
   <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application
        android:name="ai.deneb.DenebApplication"
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Starting"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:name="ai.deneb.MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Android assist gesture (홈버튼 길게 누르기 / 사이드 버튼): lets the user set
                 Deneb as the device assist app so the OS-level "call your assistant"
                 gesture opens the 업무 chat. Handled in MainActivity.handleDeepLinkIntent. -->
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- Share-sheet capture: receive text/image/audio/document shared from
                 any app into the Deneb chat. Documents ride the same capture path
                 as the in-app picker (gateway extraction) — keep the accepted set
                 in lockstep with the gateway dispatcher (document_extract.go):
                 PDF/CSV/OOXML(docx·xlsx·pptx)/Markdown. HWP stays out until the
                 gateway grows an extractor for it. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="text/csv" />
                <data android:mimeType="text/comma-separated-values" />
                <data android:mimeType="text/markdown" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />
                <!-- Legacy binary Office + RTF + ODF: the gateway converts these
                     (LibreOffice) before extracting. (HWP share is unreliable —
                     its MIME varies by app — so HWP arrives via the in-app picker,
                     which routes by extension.) -->
                <data android:mimeType="application/msword" />
                <data android:mimeType="application/vnd.ms-excel" />
                <data android:mimeType="application/vnd.ms-powerpoint" />
                <data android:mimeType="application/rtf" />
                <data android:mimeType="application/vnd.oasis.opendocument.text" />
                <data android:mimeType="application/vnd.oasis.opendocument.spreadsheet" />
                <data android:mimeType="application/vnd.oasis.opendocument.presentation" />
            </intent-filter>
            <!-- Multi-image share: several receipt photos / scan pages at once. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <service
            android:name="ai.deneb.DaemonService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- FCM: receives proactive pushes when the app is fully closed / Doze
             (the gateway's fallback when no live SSE client is connected) and
             token rotations. See FcmService.kt. -->
        <service
            android:name="ai.deneb.FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- Notification reply / mark-as-read actions (Android Auto voice reply
             + phone tray inline reply). See DenebReplyReceiver.kt. -->
        <receiver
            android:name="ai.deneb.DenebReplyReceiver"
            android:exported="false" />
        <!-- Trust Inbox: approve/reject tray buttons on work-feed approval
             card notifications. See WorkFeedActionReceiver.kt. -->
        <receiver
            android:name="ai.deneb.WorkFeedActionReceiver"
            android:exported="false" />
        <!-- Android Auto: declares this app posts Auto-compatible messaging
             notifications (read-aloud + voice reply). -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <!-- Android Auto: template car screen (read-only feed browser). The
             IOT category maps to the basic list/message template set; the
             sideloaded APK needs Android Auto developer mode's "unknown
             sources" once (single-user deployment). -->
        <service
            android:name="ai.deneb.DenebCarAppService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.IOT" />
            </intent-filter>
        </service>
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />
        <!-- Home-screen widget: next meeting + unread mail. -->
        <receiver
            android:name="ai.deneb.DenebWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/deneb_widget_info" />
        </receiver>
    </application>

</manifest>
