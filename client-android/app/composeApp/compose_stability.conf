// Third-party / framework types that are effectively stable for composition
// but the Compose compiler can't infer it.

// FileKit
io.github.vinceglb.filekit.PlatformFile

// TTS
nl.marc_apps.tts.TextToSpeechInstance

// Navigation
androidx.navigation.NavHostController

// ViewModels passed as composable parameters (reference is stable for the
// lifetime of the host composable).
ai.deneb.ui.chat.ChatViewModel
ai.deneb.ui.sandbox.SandboxFileBrowserViewModel
ai.deneb.ui.sandbox.SandboxPackagesViewModel
ai.deneb.ui.sandbox.SandboxSessionViewModel
ai.deneb.ui.settings.SandboxViewModel
ai.deneb.ui.settings.SettingsViewModel
