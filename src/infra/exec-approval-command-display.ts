// Stub: exec approval command display removed for solo-dev simplification.
export function formatExecCommandForDisplay(command: string): string {
  return command;
}
export function formatExecCommandPreview(command: string): string {
  return command;
}
