// Stub: device identity removed for solo-dev simplification.
export type DeviceIdentity = { deviceId: string; publicKey?: string };

export function loadOrCreateDeviceIdentity(): DeviceIdentity | undefined {
  return undefined;
}

export function getDeviceId(): string | undefined {
  return undefined;
}
