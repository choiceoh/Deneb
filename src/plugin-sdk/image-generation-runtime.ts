// Image generation removed — stub retained for plugin-sdk export compatibility.
export async function generateImage(): Promise<null> {
  return null;
}
export async function listRuntimeImageGenerationProviders(): Promise<never[]> {
  return [];
}
