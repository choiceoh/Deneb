import { describe, it } from "vitest";

// Skipped: provider extensions (openai) are not bundled in this repo.
describe.skip("provider runtime contract (extensions not bundled)", () => {
  it.skip("runs provider runtime", () => {});
});
