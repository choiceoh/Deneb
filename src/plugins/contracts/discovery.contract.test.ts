import { describe, it } from "vitest";

// Skipped: provider extensions (github-copilot, qwen-portal-auth, ollama, etc.) are not bundled in this repo.
describe.skip("provider discovery contract (extensions not bundled)", () => {
  it.skip("discovers providers", () => {});
});
