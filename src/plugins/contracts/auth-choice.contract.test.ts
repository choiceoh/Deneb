import { describe, it } from "vitest";

// Skipped: provider extensions (qwen-portal-auth) are not bundled in this repo.
describe.skip("provider auth choice contract (extensions not bundled)", () => {
  it.skip("resolves auth provider methods", () => {});
});
