import { describe, it } from "vitest";

// Skipped: channel extensions (discord) are not bundled in this repo.
describe.skip("plugin commands (extensions not bundled)", () => {
  it.skip("registers plugin commands", () => {});
});
