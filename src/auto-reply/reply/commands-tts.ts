// TTS commands removed — stub retained for command handler registration compatibility.
import type { CommandHandler } from "./commands-types.js";

export const handleTtsCommands: CommandHandler = async (_params, _allowTextCommands) => {
  return null;
};
