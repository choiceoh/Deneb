{
  "name": "deneb-miniapp",
  "private": true,
  "version": "0.0.1",
  "description": "Deneb Telegram Mini App — PoC frontend bundled into the gateway binary.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/telegram-web-app": "^7.10.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8"
  }
}
