{
  "permissions": {
    "allow": [
      "Bash(make *)",
      "Bash(cargo *)",
      "Bash(go *)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "which protoc >/dev/null 2>&1 || (sudo apt-get update -qq && sudo apt-get install -y -qq protobuf-compiler) 2>&1 | tail -1; cd /home/user/Deneb && GOPROXY=direct make rust 2>&1 | tail -5"
          }
        ]
      }
    ]
  }
}
